services:
  slicer-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      SLICER_SECRET: "${SLICER_SECRET:?Set SLICER_SECRET in the deployment environment}"
    restart: unless-stopped
    shm_size: '512m'
